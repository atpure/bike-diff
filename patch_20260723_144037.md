# Patch Summary: patch_20260723_144037

- **Export Date**: 2026-07-23 14:40:37 KST
- **Commit Hash**: `755d09a9cde27faa25f2048ca338f82d848feb7f`
- **Diff File**: `patch_20260723_144037.diff`

---

## 1. User Request (요청 사항)
1. `functions/index.js` 981, 1002번 라인의 `&& data.bdr_serviceCategory` 불필요 조건 제거.
2. `functions/index.js` 6276번 라인 Garmin fallback 수신 시 `bdr_serviceCategory`가 `garmin`으로 처리된 것을 `intervals`로 수정.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`functions/index.js`**:
  - 981번, 1002번 라인 `deleteActivityDoc` 조회 시 중복된 `&& data.bdr_serviceCategory` 검사 제거하여 `if (targetCategory && data.bdr_serviceCategory !== targetCategory)`로 단순화.
  - 6276번 라인의 Garmin raw 데이터 생성 시 `bdr_serviceCategory: 'garmin'`으로 설정하던 레거시 값을 서비스 표준 규격인 `'intervals'`로 수정.
