# Patch Summary: patch_20260723_141624

- **Export Date**: 2026-07-23 14:16:24 KST
- **Commit Hash**: `e63a1a2f85d2d0e5cb69381228860f49a1ee715d`
- **Diff File**: `patch_20260723_141624.diff`

---

## 1. User Request (요청 사항)
1. `functions/index.js` 3577번 라인에서 `bikeId`를 사용하고 있는데 `bdr_bikeId`를 사용해야 하는 것 아닌지 확인 후 수정.
2. `MyBikes.jsx` 893번 라인 등에서 사용 중인 `source` 변수 및 `intervals_copied`, `strava_copied` 값 등 안 쓰는 레거시 필드 잔재 삭제.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`functions/index.js`**: 3577번 라인의 `activitiesRef.where('bikeId', '==', targetBikeId)` 쿼리를 공통 표준 스키마인 `bdr_bikeId`로 변경.
- **`src/pages/MyBikes.jsx`**: 활동 복제(Clone) 로직 내 더 이상 사용하지 않던 `let source = isTargetIntervals ? 'intervals_copied' : 'strava_copied';` 변수 선언 완전 제거.
