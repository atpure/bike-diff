diff --git a/src/components/Sidebar.jsx b/src/components/Sidebar.jsx
index b5df365..c5db2ba 100644
--- a/src/components/Sidebar.jsx
+++ b/src/components/Sidebar.jsx
@@ -137,11 +137,11 @@ const Sidebar = () => {
       </nav>
 
       <div className="sidebar-footer">
-        <div className="user-profile">
+        <div className="user-profile" onClick={(e) => { e.preventDefault(); e.stopPropagation(); }}>
           <img src={user?.photoURL} alt="Avatar" />
           <div className="user-info">
             <span className="user-name">{profile?.name || user?.displayName}</span>
-            <span className="user-email">{user?.email}</span>
+            <span className="user-email">{user?.email ? user.email.replace('@', '\u200B@\u200B') : ''}</span>
           </div>
         </div>
         <div className="sidebar-actions">
