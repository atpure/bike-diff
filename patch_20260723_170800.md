# Patch Summary: patch_20260723_170800

- **Export Date**: 2026-07-23 17:08:00 KST
- **Commit Hash**: `339b01ff795d6b76158098c8bf9ab2dc346e70b2`
- **Diff File**: `patch_20260723_170800.diff`

---

## 1. User Request (요청 사항)
- 13가지 보안 및 코드 정합성 점검 항목(Firestore/Storage 보안 규칙 강화, Webhook Secret 검증, SSRF 방어, Intervals OAuth 토큰 만료 갱신 등) 검토 및 일괄 적용.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`firestore.rules` & `storage.rules`**:
  - 일반 사용자가 client SDK를 통해 프로필 문서의 `isAdmin`, `isApproved` 필드를 직접 수정/부여할 수 없도록 diff 조건 검증 규칙 추가.
  - `.exe` 설치 파일 덮어쓰기 권한을 `isAdmin()`으로 제어.
- **`functions/index.js`**:
  - `INTERVALS_WEBHOOK_SECRET` 미전송 요청 401 차단 및 Strava POST 필수 필드 검증 강화.
  - 마이그레이션 엔드포인트의 하드코딩 Fallback 키 제거.
  - `fetchAndStoreRawCompareData` URL 요청 시 허용 API 도메인 Whitelist 검증(SSRF 방어).
  - `getValidIntervalsToken`에 만료 5분 전 자동 Refresh 토큰 갱신 적용.
  - 승인 토큰 생성 시 `crypto.randomBytes(16).toString('hex')` 적용 및 알림 텍스트 `escapeHtml` 처리.
  - `importIntervalsHistoryInternal`에 `bdr_intervalsActivityId` 조회를 추가하여 웹훅으로 동기화된 활동의 이중 생성 방지.
