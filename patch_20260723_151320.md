# Patch Summary: patch_20260723_151320

- **Export Date**: 2026-07-23 15:13:20 KST
- **Commit Hashes**: `7fff05c`, `450994b`
- **Diff File**: `patch_20260723_151320.diff`

---

## 1. User Request (요청 사항)
- `findAndMergeRawData`에서 더 이상 사용하지 않는 `source` 필드 사용 부분 완전 제거 및 불필요해진 팟(stub) 코드 일괄 삭제 정리.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`functions/index.js`**:
  - `findAndMergeRawData` 내에서 `where('source', '==', 'strava')` 및 `where('source', '==', 'garmin')` 레거시 쿼리 블록 완전 삭제.
  - `isGarmin` 및 `isGarminActivity` 판단 시 더 이상 쓰이지 않는 `act.source` 파싱 로직 제거.
  - `findAndMergeRawData` 함수 정의, `needsMergeCheck` 변수, 및 `findAndMergeRawData` 호출 지점 등 미사용 데드 코드 전면 제거.
