diff --git a/src/contexts/AuthContext.jsx b/src/contexts/AuthContext.jsx
index 536fd3e..a59249a 100644
--- a/src/contexts/AuthContext.jsx
+++ b/src/contexts/AuthContext.jsx
@@ -8,16 +8,7 @@ const AuthContext = createContext();
 export function AuthProvider({ children }) {
   const [user, setUser] = useState(null);
   const [profile, setProfile] = useState(null);
-  const [loading, setLoading] = useState(() => {
-    try {
-      const hasLocalUser = Object.keys(localStorage).some(k => k.startsWith('firebase:authUser:'));
-      const hasSessionUser = Object.keys(sessionStorage).some(k => k.startsWith('firebase:authUser:'));
-      const isCallback = window.location.pathname.includes('-callback');
-      return hasLocalUser || hasSessionUser || isCallback;
-    } catch (e) {
-      return true;
-    }
-  });
+  const [loading, setLoading] = useState(true);
   const [loginLoading, setLoginLoading] = useState(false);
   const [theme, setTheme] = useState(() => localStorage.getItem('theme') || 'dark');
 
