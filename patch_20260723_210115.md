# Patch Summary: patch_20260723_210115

- **Export Date**: 2026-07-23 21:01:15 KST
- **Commit Hash**: `a43547f07aa8631c78d094022b7aca5fcbdc61c8`
- **Diff File**: `patch_20260723_210115.diff`

---

## 1. User Request (요청 사항)
- Strava 역사 가져오기(Import History) 진행 시 전체 개수(예: 라이딩 901개)를 기준으로 프로그레스바가 정상 상승하여 진행율을 볼 수 있도록 반영 조치 요청.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`functions/index.js`**:
  - `importStravaHistoryInternal`에서 `stravaAthleteId` 미등록 시 `/api/v3/athlete`를 우선 호출하여 숫자 Athlete ID(예: 35866213)를 보장 확보.
  - Strava Stats API(`all_ride_totals.count`)에서 라이딩 수치(예: 901개)를 추출하고, 가져오기 시작 즉시 `importStatus`에 `total` 수치를 등록하도록 보완.
- **`src/pages/Activities.jsx`**:
  - 프로그레스바 백엔드 전달 수치 계산 공식을 `count / processed`에서 `processed / total` (예: `스캔개수 / 901`)로 수정하고 텍스트 레이블을 `x / y scanned (z imported)` 형태로 직관화.
- **`scripts/get_strava_count.cjs`**:
  - Strava 전체 개수 조회를 위한 검증용 스크립트 신규 추가.
