# Patch Description - 2026-07-26 01:56:51

## Overview of Changes

1. **Refined SVG Icon Components**:
   - **`IntervalsIcon.jsx`**: Created and tuned official double-peak SVG icon with uniform stroke thickness (`strokeWidth="2.6"`) and edge-to-edge horizontal baselines.
   - **`StravaIcon.jsx`**: Re-architected exact geometric official Strava logo icon with symmetrical upper/lower chevrons and seamless flush Y-axis meeting point.

2. **Rider-Centric Terminology Standardization**:
   - Updated `Initial Distance (km)` ➔ **`Initial Mileage (km)`** in Manage Parts & Trainer modals.
   - Updated `Replacement Interval (km)` ➔ **`Service Interval (km)`** in Manage Parts modals and CSV exporter.
   - Updated `Linked to` ➔ **`Mounted on`** in Manage Parts modals.
   - Updated `Action` ➔ **`Service Type`** in History modals and History table header.
   - Updated section card title to **`Register Ride to Work Activity`** in Settings page.

3. **Debug Icon Permission Guard**:
   - Restricted raw DB payload viewer buttons (`<Database />`) on Reconcile (Intervals) page to **Admin users** with **Display Debug Info** enabled (`profile?.isAdmin && profile?.showDebugInfo`).

4. **MyBikes Clone & Navigation Logic**:
   - Updated BikeCard icon click handler:
     - When BOTH STRAVA and Intervals.icu services are active in Settings, clicking the bike icon opens the Bike Clone modal (`onClone`).
     - When either service is inactive, clicking the bike icon triggers `onClick()`, navigating directly to the **Manage Parts** page for that bike.

---

## File Changes Summary

- **NEW** `src/components/icons/IntervalsIcon.jsx`
- **NEW** `src/components/icons/StravaIcon.jsx`
- **MODIFY** `src/pages/MyBikes.jsx`
- **MODIFY** `src/pages/ManageParts.jsx`
- **MODIFY** `src/pages/History.jsx`
- **MODIFY** `src/pages/Settings.jsx`
- **MODIFY** `src/pages/Reconcile.jsx`
- **MODIFY** `src/components/modals/AddTrainerModal.jsx`
- **MODIFY** `src/components/modals/EditTrainerModal.jsx`
- **MODIFY** `refdata/rawdatainfo.md`
