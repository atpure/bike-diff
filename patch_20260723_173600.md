# Patch Summary: patch_20260723_173600

- **Export Date**: 2026-07-23 17:36:00 KST
- **Commit Hash**: `1bf6fe74fafd07c168379411ef42643e4a54031f`
- **Diff File**: `patch_20260723_173600.diff`

---

## 1. User Request (요청 사항)
- 레거시 지원 및 마이그레이션 예외 처리가 더 이상 불필요하므로, `deleteActivityDoc` 등 코드에서 `intervals_` 문서 ID로 비교하거나 검사하는 레거시 조건을 모두 제거할 것.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`functions/index.js`**:
  - `deleteActivityDoc` 내에서 문서 ID를 `intervals_12345` 또는 `strava_12345` 형태로 조합하여 조율하던 `d.id === cleanId || d.id === '${targetCategory}_${cleanId}'` 및 `startsWith('intervals_')` 레거시 대조 조건을 완전히 삭제.
  - 문서 ID 조회를 단순 `d.id === actDocId` (Firestore 순수 생성 ID) 및 `bdr_intervalsActivityId` 필드 조회 기반으로 최종 단순화.
