# Patch Summary: patch_20260726_003717

- **Export Date**: 2026-07-26 00:37:17 KST
- **Commit Hash**: `c356090d8f051cbc8be6f2cad80538102a9e666a`
- **Diff File**: `patch_20260726_003717.diff`

---

## 1. User Request (요청 사항)
- Settings 페이지의 Service Activation 설정 중 Intervals.icu 및 STRAVA 활성화 옵션이 둘 다 동시에 해제되지 않도록 방지하고 안내 Toast 팝업 띄우기.
- My Bikes, Manage Parts, Activities, History 페이지의 온보딩/안내 문구를 활성화된 서비스 옵션 상태에 따라 동적으로 노출되도록 반영.
- Settings 페이지에서 `Exclude commute activities from notifications` 옵션이 비활성화 상태일 때 클릭 시 `Notify on new Intervals.icu activities` 옵션을 먼저 활성화하라는 영문 Toast 팝업 출력.
- Settings 페이지 Connected Services 카드 내 STRAVA Connect 버튼 색상/스타일을 Intervals.icu Connect 버튼과 동일하게 통일.

---

## 2. Rationale & Modifications (수정 내용 및 이유)

- **`src/pages/Settings.jsx`**:
  - `handleServiceToggle`: `isIntervalsActive`와 `isStravaActive` 중 하나만 활성화된 상태에서 나머지 1개를 해제하려고 할 때 토글을 차단하고 `showToast('At least one service (Intervals.icu or STRAVA) must remain enabled.', 'warning')`를 발생시킴.
  - `Exclude commute activities from notifications` 옵션 래퍼 div에 `onClick` 이벤트 및 `pointerEvents` 처리 추가. 상위 알림 설정이 꺼져 있을 때 클릭하면 `showToast('Please enable "Notify on new Intervals.icu activities" option first.', 'info')`를 띄움.
  - Connected Services 섹션의 STRAVA Connect 버튼 `className`을 `btn-primary`로 변경하고 커스텀 주황색 배경 스타일을 제거하여 Intervals.icu Connect 버튼과 통일감을 부여함.

- **`src/pages/MyBikes.jsx`**:
  - 온보딩 안내 문구를 `serviceSettings` 상태에 따라 동적 생성:
    - 둘 다 활성화: `"Connect your Intervals.icu or STRAVA account to import your bikes and start tracking."`
    - Intervals.icu만 활성화: `"Connect your Intervals.icu account to import your bikes and start tracking."`
    - STRAVA만 활성화: `"Connect your STRAVA account to import your bikes and start tracking."`

- **`src/pages/ManageParts.jsx`**, **`src/pages/Activities.jsx`**, **`src/pages/History.jsx`**:
  - `onSnapshot` 기반 `serviceSettings` 수신 이펙트 추가.
  - 데이터가 없는 초기 온보딩 상태 문구에서 `STRAVA or Intervals.icu`, `Intervals.icu`, `STRAVA` 명칭을 활성화 옵션에 맞춰 동적으로 노출하도록 수정.
