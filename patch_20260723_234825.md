# Patch Summary: patch_20260723_234825

- **Export Date**: 2026-07-23 23:48:25 KST
- **Commit Hash**: `d0174cc8963eda380afad0c36527810a819be621`
- **Diff File**: `patch_20260723_234825.diff`

---

## 1. User Request (요청 사항)
- Settings 페이지의 "Rename commutes to 'Ride to Work' / 'Ride Home'" (`markCommuteActivities`) 및 "Prefix area name for rides away from home(1 km)" (`appendLocationName`) 옵션 조건 관계 확인 및 출퇴근 라이딩(출근/퇴근) 우선 판별 자동 이름 변경 반영.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`functions/index.js`**:
  - `getCommuteType(startLat, startLng, endLat, endLng, commute)` 헬퍼 함수 추가:
    - 집 $\rightarrow$ 회사 (500m 이내): `'work'` (출근)
    - 회사 $\rightarrow$ 집 (500m 이내): `'home'` (퇴근)
  - `isCommuteActivity` 함수가 `getCommuteType`을 재사용하도록 개편.
  - `autoRenameIntervalsActivityIfCycling` 함수 시그니처에 도착지 좌표(`endLat`, `endLng`)를 추가하고, 판별 조건의 우선순위를 엄격하게 정리:
    1. **`markCommuteActivities` ON**: 출근 시 `"출근 라이딩"`, 퇴근 시 `"퇴근 라이딩"` 적용.
    2. **`appendLocationName` ON**: 출발지가 집 1 km 초과일 경우 역지오코딩을 통한 `"{지역명} {시간대 라이딩}"` (예: `"서초구 야간 라이딩"`) 적용.
    3. **기본값**: `"{시간대 라이딩}"` (예: `"야간 라이딩"`) 적용.
  - 활동 신규 등록 및 업데이트 호출부에 `endLat`, `endLng`를 모두 전달하도록 수정.
