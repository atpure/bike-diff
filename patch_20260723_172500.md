# Patch Summary: patch_20260723_172500

- **Export Date**: 2026-07-23 17:25:00 KST
- **Commit Hash**: `204e639e3dde9f842465bb763fd58b8a3c15f627`
- **Diff File**: `patch_20260723_172500.diff`

---

## 1. User Request (요청 사항)
1. 자전거 삭제 시 기존 동작 방식(연결 활동 일괄 삭제)으로 원복 및 명세서(`docs/specifications.md`) 수정.
2. `importIntervalsHistoryInternal` 및 `processNewIntervalsActivity`에서 `intervals_` 접두 문서 ID 처리 코드 전면 삭제.

---

## 2. Rationale & Modifications (수정 내용 및 이유)
- **`src/contexts/BikeContext.jsx` & `docs/specifications.md`**:
  - 자전거 삭제 시 연결된 모든 활동(`bdr_bikeId == bikeId`)을 일괄 삭제하도록 `deleteBike` 원복 및 문서 명세 수정.
- **`functions/index.js`**:
  - `importIntervalsHistoryInternal` 및 `processNewIntervalsActivity`에서 `intervals_${act.id}` 형태의 문서 ID 생성 및 조회 로직 제거.
  - 활동 저장 시 Firestore 자동 생성 ID(`doc()`)를 사용하고, 가져오기/동기화 중복 여부 확인 시 `bdr_intervalsActivityId` 필드 조회만을 수행하도록 통일.
